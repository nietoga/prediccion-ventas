services:
  server:
    image: anietog1/prediccion-ventas:latest
    restart: always
    environment:
      - DATABASE_SOURCE=s3://predecir-ventas-data/bdCompletarAgrupRefinada.csv
    ports:
      - 80:80
